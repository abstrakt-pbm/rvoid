[package]
name = "rvoid"
version = "0.0.1"
edition = "2024"

[dependencies]
rvoid-core = { path = "core" }
rvoid-startup = { path = "startup" }

[features]
default = []

uefi = [
    "rvoid-startup/uefi",
]
