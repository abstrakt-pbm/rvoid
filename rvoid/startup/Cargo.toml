[package]
name = "rvoid-startup"
version = "0.0.1"
edition = "2024"

[dependencies]
rvoid-stub-entry = { path = "stub/entry_macro" }

rvoid-uefi = { path = "uefi", optional = true }

[features]
default = []

uefi = [
    "dep:rvoid-uefi",
]
